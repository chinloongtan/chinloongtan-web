webpackJsonp([0xc6b09d98388b],{509:function(e,n){e.exports={data:{markdownRemark:{html:'<p>Having toying around with RASA bot for a couple of days, I adopted the open source conversational AI framework into a workflow of one of my project.\nThe project requires a chatbot with interactive menu in the UI to get the automation done, and RASA bot is the perfect candidate for that.</p>\n<br />\n<h4>Setup</h4>\n<p>The setting up of RASA project requires <code>python</code> envinronment and tooling.</p>\n<p>This is what works for me.</p>\n<pre><code>  python3.8 -m pip install virtualenv\n  mkdir rasa-bot\n  cd rasa-bot\n  python3.8 -m virtualenv venv\n  source venv/bin/activate\n  python3.8 -m pip install rasa\n  rasa --version\n  rasa init\n</code></pre>\n<br />\n<h4>Additional frequently used commands</h4>\n<ol>\n<li>I added <code>--enable-api</code> because my application mainly communicate via APIs.</li>\n</ol>\n<pre><code>  rasa run --enable-api\n</code></pre>\n<ol start="2">\n<li>After making changes to the nlu.yml</li>\n</ol>\n<p><code>rasa train</code></p>\n<br />\n<h4>Chat in action</h4>\n<p>My application can start replying to conversations by sending the message to my RASA bot server.</p>\n<p><code>POST http://0.0.0.0:5005/webhooks/rest/webhook</code></p>\n<pre><code>{\n  "sender": "test_user",\n  "message": "Hi"\n}\n</code></pre>\n<hr>\n<p>Sample response</p>\n<pre><code>[\n    {\n        "recipient_id": "test_user",\n        "text": "Hey! How are you?"\n    }\n]\n</code></pre>\n<p>That\'s a good start, since I can now reply to a conversation from the response from RASA bot.</p>\n<p>We want now move on to train the bot for more specific use case by determining the intent of the message.</p>\n<p>And we can do this by editing the file in <code>data/nlu.yml</code>.</p>\n<p>For example, add</p>\n<p><code>- salam sejahtera</code></p>\n<p>to</p>\n<p><code>- intent: greet</code></p>\n<p>and run <code>rasa train</code> to update the model.</p>\n<p>We can also define form depending on the business logic to handle more specific use case. Restaurant reservation/ordering for example.</p>\n<br />\n<p>That\'s all for today!</p>\n<p>Cheers.</p>',frontmatter:{date:"October 27, 2022",path:"/blog/first-try-rasa-chat-bot",title:"First try with RASA chatbot",excerpt:"Conversation automation",tags:["backend"]}}},pathContext:{}}}});
//# sourceMappingURL=path---blog-first-try-rasa-chat-bot-521f79ad7d231a89e3e6.js.map